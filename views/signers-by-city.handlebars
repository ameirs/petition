<header></header>
<p> {{cityName}} 🏩 🦔 </p>
{{#rows}}
    <div>
        <p>{{#if link}}
                <a href="{{link}}"> {{first}} {{last}}</a>
            {{else}}
                {{first}}
                {{last}}
            {{/if}}
        </p>
    </div>
{{/rows}}